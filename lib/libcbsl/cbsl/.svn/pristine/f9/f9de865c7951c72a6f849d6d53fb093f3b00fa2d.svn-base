#ifndef HEXDUMP_H
#define HEXDUMP_H

#include <stdio.h>

#ifdef __cplusplus
# define EXTERN extern "C"
#else
# define EXTERN extern
#endif

EXTERN void hexdump(
        char *str                   ,
        size_t len                  ,
        void const *buf             ,
        unsigned int const sz       ,
        unsigned int const row_len
                   );

#endif
